# CLAUDE.md

このリポジトリのエージェント向け指示は `AGENTS.md` に集約している。

このプロジェクトは最終的に eve ランタイム上で動かす想定なので、指示ファイルは eve の
標準である `AGENTS.md` を実体とする。ただし現時点（Phase 1）ではエンジンとして
Claude Code を使っているため、その入口としてこのファイルを置いている。

指示を追記・修正するときは `AGENTS.md` を編集すること。このファイルは入口のまま維持する。

@AGENTS.md
