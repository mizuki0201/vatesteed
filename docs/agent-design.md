# エージェント設計

> **このドキュメントは大部分が未確定。** 引き継ぎ資料の時点で「最も手つかず」の領域。
> 下の内容を確定として扱わないこと。

## 前提（確定）

- skills は `agent/skills/` に置き、eve の `SKILL.md` 規約に従う（Agent Skills 標準と互換）
- skills はエージェント単位でスコープされる。subagent の skills はルートから見えず、逆も同じ。
  共有したい実行コードは `lib/` に置く
- `agent/instructions.md` は常時読み込み。短く安定した内容（identity と standing rule）だけに
  保ち、長い手順や状況依存の手順は skills に置く

## 機能領域（暫定案）

**分割粒度ごと見直す前提の暫定案。この単位でサブエージェントや skill を切ると決めたわけでは
ない。**

- 各馬の過去走を1走ずつ、内容・展開・不利・相手関係を踏まえて評価する
- ローテーション・調整過程・輸送や遠征の影響を評価する
- 相手関係からレースレベルを推定する
- 適性（距離・馬場・コース）を見立てる
- それらを統合して能力値・評価に変換する
- 騎手・陣営のレース後コメントを蓄積し、含意を読み取って評価に反映する

再現したい思考の全体像は [concept.md](concept.md#再現したい思考の範囲) を参照。
そこにも書いたとおり、列挙は例示であって網羅ではない。

## 決まったこと（2026-08-08）

スキーマ設計のブレストで、この3つが決まった。

### 馬プロファイルの蓄積先と更新

- 蓄積先は `horse_notes`（[data-model.md](data-model.md#評価)）。**1頭につき1行を上書き**する
- **AI と人間の対話で作る。** AI が過去の出走の評価を読んで案を出し、人間がすり合わせて結論を
  残す。やり取りの過程は残さない
- 更新のきっかけは2つ。**レース後**（必ず起きる）と、**予想の会話の中**（決まったとき）
- したがって勝手には増えない。対話した馬の分だけ溜まる

### 血統分析の範囲

- **5〜6世代を遡るのはマスト**
- **兄弟系は任意。** 見たい対象ではあるが、現実的にどこまで追えるか未知数のため必須にしない
- どこまで見たかは `pedigree_notes.scope` に自由記述で残す（「6代」「6代+全兄弟」など）

### Phase 1 で DB を触る形（2026-08-09）

**最初の1レースは生SQLで登録する。** `getSql()` に直接 SQL を投げる。目的別の関数は作らない。

理由は、**関数に何が要るかは1レース登録してみないと分からない**ため。先に設計すると使われない
ものを作ることになる。手で書いてみて面倒だった部分が、そのまま最初の関数になる。

**したがって「1レース登録したあとに `lib/` の関数を切り出す」という宿題が残っている。**
面倒だったところを覚えておくこと。ここを飛ばすと生SQLのまま固定される。

`lib/` の切り方そのものは、この切り出しのときに決まる（下記「未確定事項」）。

### ナレッジ登録の導線

- **当面は Claude Code 経由**。スマホから投げる形は想定しない
- 改善したくなった時点で改めて方針を考える

## 作った skill（2026-08-09）

| skill | 役割 |
| --- | --- |
| [task-management](../agent/skills/task-management/) | `docs/tasks/` にタスクを起票し、状態と内容を更新し、完了にする |

`task-management` は**予想の中身とは関係ない**。Claude Code から skill が動くことを確かめる
ための1本目として作り、そのまま作業管理に使っている。ここから「skills の粒度」が決まった
わけではない（下記「未確定事項」のまま）。

タスクは**1タスク1ファイルでフラットに並べ、`area` と `status` は frontmatter で持つ**。
`docs/tasks/` は gitignore してあり、コミットしない。

Claude Code へのブリッジの張り方は
[architecture.md](architecture.md#claude-code-へのスキルのブリッジ) を参照。

## 未確定事項

**勝手に確定させないこと。**

- サブエージェントの分割粒度
- **サブエージェントの命名規則（未合意）**
- skills の粒度
- `instructions.md` の内容
- 「叩き / 本番」「調教の仕上がり」などの評価軸を、どの情報源からどう判断するか
- エージェントの自律的提案の枠組み（どこまで提案していいか、暴走を防ぐ範囲）

## 運用まわりの未確定事項

- 予想の実行トリガー（枠順確定をどう検知するか）
- 記事生成の流れ（思考ログ → note / Zenn 草稿への変換層）
- 自分の購入が実際の馬券購入なのか、AI と同じく擬似なのか
- `lib/` の切り方（`agent/tools/` と Next.js の両方から呼ばれる前提での責務分割）。
  **1レース登録したあとに、面倒だったところを関数へ切り出す時点で決める**

### `lib/` に必要になるロジック（メモ）

切り方を決めるのは先だが、**何が要るかは分かっている**ので忘れないように書いておく。
いずれも [data-model.md](data-model.md#購入) の決定から直接出てくるもの。

**2026-08-09 に大半を実装した。** 並べ方は `AGENTS.md` の「`lib/` の並べ方」に従い、関心ごとに
1ディレクトリで置いている。**どこまでを1つの関心に含めるかは未確定**なので、あとで分けることに
なっても済むよう、どちらも DB に触らない純粋な計算だけにしてある。

| 用途 | 実装 |
| --- | --- |
| 買い目の展開 / 点数と金額 / 的中の判定 / 複勝の的中上限 / 回収率 | [lib/bets/](../lib/bets/) |
| レース名の正規化（全角→半角） | [lib/race-name/](../lib/race-name/) |
| 各列に入っていい値（[data-model.md](data-model.md#入っていい値) の16グループ） | [lib/enums/](../lib/enums/) |
| 印の表示 | **未実装。** 予想を入れる段階まで要らない |
| レース名の別名マスタ（「春天」→「天皇賞(春)」） | **未作成。** 略称の展開は1対1で決まらないのでマスタが要る |

| 用途 | やること |
| --- | --- |
| 買い目の展開 | `bet_legs` の列から実際の買い目を組み立てる。列の直積 → 同じ馬の重複を捨てる → マルチなら並べ替えを加える → 順不同の券種は顔ぶれで1点にまとめる |
| 点数と金額 | 展開した件数を `combination_count` に、`unit_amount × 件数` を `total_amount` に入れる。**手で入れない** |
| 的中の判定 | 「買った馬を着順の小さい順に並べたとき、i番目の着順が i 以下」。複勝とワイドだけ「n着以内に入っていればいい」型で別扱い |
| 複勝の的中上限 | 出走頭数が8頭以上なら3着、7頭以下なら2着 |
| 回収率 | `(payout + refund) ÷ total_amount` |
| 印の表示 | `marks` の `name` と `symbol` のどちらからでも引けるようにする |
| レース名の正規化 | 「春天」→「天皇賞(春)」。md マスタを引いてから SQL を投げる。あわせて**全角の括弧・数字・英字を半角へ寄せる**（JRA 公式サイトは全角括弧のため、写すと混ざる） |

**書くときに気をつける点が1つ。** 複勝の的中上限を出す「出走頭数」は、`entries.status` が
**取消・除外以外**の行数で数える。着順が入っている行だけを数えると、**中止**の馬が漏れて
頭数を1つ少なく見積もる。

## 決まっている運用

- **枠順確定直後**にエージェントを動かし、その時点で分かる範囲を記事化する
- 馬券は実際に買わず、**購入ポートフォリオだけ出力して擬似的に購入**し回収率を計測する
