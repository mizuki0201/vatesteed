# 法務・コンプライアンス

## 確定している方針

- **JRA-VAN は商用利用不可。**
- スクレイピングの論点は3層に分けて考える
  1. **取得行為** — 各サイトの利用規約
  2. **蓄積・解析** — 著作権法30条の4
  3. **出力・販売** — 引用要件
- **採用方針: 自分の目で見て、自分の言葉で要約・評価した内容をナレッジに登録する**
  - 規約・著作権どちらの論点も薄くなる
  - 副産物として「自分のフィルターを通した解釈」が蓄積され、コンセプトとも合致する
- **エージェントへの強制は `lib/` 側でのドメイン検査で行う**（2026-08-11 決定）。
  instructions やスキルへの記述だけに頼らない。集める手順はスキルに置くが、**アクセスしてよいか
  の検査は `lib/` に置き、誰が呼んでも通る**ようにする。経緯は
  [agent-design.md](agent-design.md#集めること2026-08-11-決定)
- 販売時は**景品表示法**（回収率の誇大表示）にも注意する

## 自動アクセス禁止リスト

サイトごとに「自動収集の可否」「転載可否」「確認日」を管理する。エージェントにも持たせ、
**NG ドメインには自動アクセスさせない**。

| ドメイン | 自動収集 | 転載 | 確認日 | 備考 |
| --- | --- | --- | --- | --- |
| _(未記入)_ | | | | |

> **このリストはまだ空。** 埋めるまでは「自分の目で見て自分の言葉で要約する」原則を
> 全面的に適用し、エージェントからの自動アクセスは行わない。

## 未確定事項

- リストの管理場所（このファイルか、DB か、`lib/` の定数か）。**検査を `lib/` に置くと決めた
  ので、リストも `lib/` に寄せるのが素直だが、確定はしていない**
- 確認日の棚卸し頻度
